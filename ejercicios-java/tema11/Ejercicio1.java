/**
 * En determinados contextos, por ejemplo cuando la información va a ser utilizada en el entorno
 * educativo, es frecuente censurar ciertos términos para que no aparezcan en los documentos o en las
 * páginas web. Realiza un programa que sea capaz de leer un fichero de texto, encontrar las palabras
 * que se quieren censurar y generar otro fichero como resultado en el que se sustituyan las palabras
 * censuradas por equis (X). Por ejemplo, si en el fichero aparece la palabra prohibida “culete”, en el
 * fichero censurado aparecerá como XXXXXX. Las palabras prohibidas están guardadas en un fichero
 * de texto (una palabra por línea). Los nombres de los ficheros se pasan como argumentos por la línea
 * de comandos.
 * En caso de que no se le pasen los tres nombres de fichero al programa, debe emitir un mensaje de
 * error.
*/
public class Ejercicio1 {
    public static void main(String[] args) {
        
    }
}
